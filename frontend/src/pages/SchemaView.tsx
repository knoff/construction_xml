export default function SchemaView() {
  // Placeholder: backend пока не даёт отдельный /schemas/{id}, показываем список/загрузку
  return <div className="text-sm text-neutral-500">Детальный просмотр будет добавлен, когда появится эндпоинт /schemas/{id}.</div>
}
