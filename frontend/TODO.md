# TODO: Наведение порядка во фронтенде

## Общие проблемы и направления работ

1. **API и сетевой слой**
   - Ввести единый HTTP-клиент (axios) с базовой конфигурацией, перехватчиками и типизацией.
   - Вынести вызовы API из компонентов в сервисы/адаптеры, устранить дублирование логики `fetch` + обработка ошибок.

2. **Структура проекта и архитектура**
   - Перейти на feature-based структуру: `features/<feature>/{api,components,pages,hooks}`.
   - Навести порядок между `pages`, `features`, `components`, исключить божественные компоненты.
   - Выделить контейнеры (загрузка данных) и презентационные компоненты для крупных страниц.

3. **Типизация данных**
   - Описать ответы API (интерфейсы или валидаторы), отказаться от `any`.
   - Ввести общие типы для документов, файлов, версий и использовать их везде.

4. **Управление состоянием и навигация**
   - Использовать `useNavigate` вместо `window.location`.
   - Рассмотреть внедрение React Query / Zustand / RTK Query для единообразного состояния и кеширования.

5. **UI-компоненты и стиль**
   - Консолидировать диалоги, кнопки, формы; обеспечить единый UX.
   - Вынести типографику Markdown и токены Tailwind в отдельные модули/плагины.

6. **Оптимизация бандла**
   - Настроить ленивую загрузку маршрутов, анализ бандла и tree-shaking.
   - Проверить использование тяжёлых зависимостей и альтернатив.

7. **Инструменты качества**
   - Добавить ESLint, Prettier, единые правила форматирования и lint-скрипты.
   - Настроить тесты (Vitest/Jest, React Testing Library) для критичных компонентов.
   - Интегрировать проверки в CI.

8. **Документация и onboarding**
   - Обновить README фронтенда с инструкциями.
   - Задокументировать архитектурные принципы, слой API, шаблоны компонентов.
