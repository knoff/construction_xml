# === Application ports (host -> container) ===
APP_PORT="18080"        # host port
APP_PORT_INTERNAL="8000"

# === Postgres (host -> container) ===
PG_PORT="15433"         # host port
PG_DB="xmlsvc"
PG_USER="<DB_USER_NAME>"
PG_PASSWORD="<DB_PASSWORD>"

# === MinIO (host -> container) ===
MINIO_PORT="19000"            # host port
MINIO_CONSOLE_PORT="19001"    # host port
MINIO_ROOT_USER="<MINIO_USER_NAME>"
MINIO_ROOT_PASSWORD="<MINIO_USER_PASSWORD>"
S3_BUCKET="xmlsvc"

# === Misc ===
# You can safely change these values if needed; compose picks them up.
# For Windows PowerShell: setx commands persist env vars, but using this .env with docker compose is preferred.
